# 🍎 macOS 部署指南

> 本指南将帮助你 在 Mac 电脑上 安装 OpenClaw。即使你从未接触过编程，也能跟着步骤完成！

---

## 📋 目录

1. [环境要求](#环境要求)
2. [前置条件](#前置条件)
3. [详细安装步骤](#详细安装步骤)
4. [验证安装](#验证安装)
5. [常见问题](#常见问题)

---

## 🖥️ 环境要求

| 项目 | 最低配置 | 推荐配置 |
|------|---------|---------|
| 操作系统 | macOS 11 (Big Sur) | macOS 13+ |
| 内存 | 4GB | 8GB+ |
| 存储空间 | 5GB | 10GB+ |
| 网络 | 需要联网 | 需要联网 |

---

## ✅ 前置条件

在开始之前，你需要准备：

1. ✅ **Apple ID** - 用于安装 Xcode Command Line Tools
2. ✅ **网络连接** - 需要访问 GitHub
3. ✅ **管理员权限** - 安装软件时需要输入密码

---

## 📝 详细安装步骤

### 第 1 步：打开终端 (Terminal)

> 💡 **提示**：终端是 macOS 自带的命令行工具

1. 点击屏幕左上角的 **🔍 搜索图标** (放大镜)
2. 输入 `Terminal` 或 `终端`
3. 点击 **终端.app**

![终端图标](https://via.placeholder.com/150x150?text=Terminal+Icon)

---

### 第 2 步：安装 Homebrew (包管理器)

> 💡 **提示**：Homebrew 是 macOS 的软件包管理器，帮助你轻松安装各种工具

在终端中粘贴以下命令，然后按 **回车键**：

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

📌 **注意**：
- 安装过程中需要输入你的电脑密码（输入时不显示字符是正常的）
- 看到 `Installation successful!` 即表示安装成功

---

### 第 3 步：安装必要软件

在终端中依次执行以下命令：

#### 3.1 安装 Git（版本控制工具）
```bash
brew install git
```

#### 3.2 安装 Node.js（运行环境）
```bash
brew install node
```

#### 3.3 安装 Python（编程语言）
```bash
brew install python
```

📌 **验证安装**：安装完成后，在终端输入以下命令检查版本：
```bash
git --version
node --version
python3 --version
```

如果看到版本号（如 `git version 2.40.0`），说明安装成功！

---

### 第 4 步：克隆 OpenClaw 仓库

> 💡 **提示**：克隆就是把 GitHub 上的代码下载到你的电脑

1. 创建放置代码的文件夹：
```bash
mkdir -p ~/OpenClaw
cd ~/OpenClaw
```

2. 从 GitHub 克隆代码：
```bash
git clone https://github.com/Manstein17/--botbot.git .
```

📌 **注意**：
- 看到 `Cloning into '...'...` 表示正在下载
- 看到 `Checking out files...` 表示下载完成

---

### 第 5 步：安装项目依赖

进入项目目录并安装所需的其他软件：

```bash
cd ~/OpenClaw
npm install
```

📌 **提示**：
- 这个过程可能需要几分钟
- 看到 `added XXX packages` 表示安装成功

---

### 第 6 步：配置 OpenClaw

1. 复制配置文件：
```bash
cp openclaw-config.json.example openclaw-config.json
```

2. 用文本编辑器打开配置文件：
```bash
open -a TextEdit openclaw-config.json
```

3. 根据你的需求修改配置（可选）

---

### 第 7 步：启动 OpenClaw

终于到了启动的时刻！🎉

```bash
openclaw gateway start
```

如果看到类似以下信息，说明启动成功：

```
🚀 OpenClaw Gateway 正在启动...
✅ Gateway 已启动，监听端口: 3000
```

---

## ✅ 验证安装

### 方法 1：检查进程是否运行

```bash
openclaw gateway status
```

### 方法 2：访问网页

1. 打开浏览器（Safari 或 Chrome）
2. 在地址栏输入：`http://localhost:3000`
3. 如果看到 OpenClaw 界面，说明安装成功！

---

## ❓ 常见问题

### Q1: 安装 Homebrew 时卡住了怎么办？

**解决方法**：
1. 按 `Ctrl + C` 取消安装
2. 重新运行安装命令
3. 如果仍然卡住，尝试使用国内镜像：

```bash
/bin/bash -c "$(curl -fsSL https://ghproxy.com/https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

---

### Q2: npm install 报错怎么办？

**常见错误**：
```
npm WARN deprecated ...
npm ERR! code ELIFECYCLE
```

**解决方法**：
1. 清理缓存：`npm cache clean --force`
2. 删除 node_modules：`rm -rf node_modules`
3. 重新安装：`npm install`

---

### Q3: 端口被占用怎么办？

**错误信息**：
```
Error: listen EADDRINUSE: address already in use :::3000
```

**解决方法**：
1. 查找占用端口的进程：
```bash
lsof -i :3000
```

2. 关闭该进程：
```bash
kill -9 <PID>
```

3. 重新启动 OpenClaw：
```bash
openclaw gateway start
```

---

### Q4: 如何更新 OpenClaw？

```bash
cd ~/OpenClaw
git pull
npm install
openclaw gateway restart
```

---

### Q5: 如何卸载 OpenClaw？

```bash
cd ~/OpenClaw
rm -rf *
cd ~
rm -rf OpenClaw
npm uninstall -g openclaw
```

---

## 📞 获取帮助

如果遇到无法解决的问题：

1. 📖 查看 [OpenClaw 官方文档](https://github.com/Manstein17/--botbot)
2. 💬 在 GitHub 提交 [Issue](https://github.com/Manstein17/--botbot/issues)
3. 🔧 检查网络连接是否正常

---

## 🎉 恭喜！

你已成功在 macOS 上部署 OpenClaw！现在可以开始使用了。

**下一步**：
- 📖 阅读 SOUL.md 了解 OpenClaw 的工作方式
- ⚙️ 配置你需要的插件和功能
- 🎮 开始使用 OpenClaw！
